/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Using the app router is default in recent Next versions; remove experimental flags.
};
module.exports = nextConfig;
